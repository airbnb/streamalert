{
  "enabled": false,
  "config": {
    "destination_kinesis": "KINESIS_STREAM",
    "sfn_timeout_secs": 300,
    "sfn_wait_secs": 30
  },
  "packs": {
    "sample": {
      "description": "Runs sample queries once per hour",
      "schedule_expression": "rate(1 hour)"
    }
  },
  "lambda_config": {}
}